--- src/pages/meal-prep/index.astro
+++ src/pages/meal-prep/index.astro
@@ -7,6 +7,10 @@

 const categories = ['All', ...new Set(sorted.map(mp => mp.data.category))];

+const renderedMPs = await Promise.all(
+  sorted.map(async (mp) => ({ mp, rendered: await mp.render() }))
+);
+
 const categoryLabels: Record<string, string> = {
   writing: '✍️ Writing',
   coding: '💻 Coding',
@@ -73,8 +77,9 @@
     <!-- Cards -->
     {sorted.length > 0 ? (
       <div class="mp-grid" id="mp-grid">
-        {sorted.map(async mp => {
-          const { Content } = await mp.render();
+        {renderedMPs.map(item => {
+          const mp = item.mp;
+          const { Content } = item.rendered;
           return (
             <div
               class="mp-card"
